import styled from 'styled-components/native';

const Input = styled.TextInput({
  background: '#C2C2C2',
  borderRadius: '10px',
  fontSize: '25px',
  height: '50px',
  width: '200px',
  marginBottom: '20px',
});

export default Input;
